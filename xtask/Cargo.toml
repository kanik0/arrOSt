[package]
name = "xtask"
version = "0.1.0"
edition = "2024"

[dependencies]
anyhow = "1"
bootloader = { version = "0.11.15", default-features = false, features = ["uefi"] }
arrostd = { path = "../crates/arrostd" }
arrost-user-init = { path = "../user/init" }
arrost-user-doom = { path = "../user/doom" }
